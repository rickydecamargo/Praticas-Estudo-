# Obtendo o dobro, o triplo e a raiz quadrada de um número

n = int(input('Digite um valor: '))
d = n * 2
t = n * 3
r = n ** (1/2)
print('O dobro de {} vale {}. ' .format(n, d))
print('o triplo de {} vale {}. ' .format(n, t))
print('a raiz quadrada de {} vale {:.2f}. ' .format (n, r))
